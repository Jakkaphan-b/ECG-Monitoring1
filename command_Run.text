1. รัน Frontend (หลัก)
npm install
npm run dev

2. รัน Firebase Functions (ถ้าต้องการ)
cd functions
npm install
firebase emulators:start --only functions

หรือถ้า deploy แล้วให้ใช้:
firebase deploy --only functions

3. รัน LINE Bot Test Server (ถ้าต้องการทดสอบ)
cd line-bot-test
npm install
npm start

4.รัน Email Server (Backend)
# เปิด Terminal หรือ Command Prompt
# ไปที่ folder email-server
cd e:\github\ECG-Monitoring1\email-server

# รัน server
node server.js

# Terminal 1: Email Server
cd e:\github\ECG-Monitoring1\email-server && node server.js

# Terminal 2: React App  
cd e:\github\ECG-Monitoring1 && npm run dev

 cd backend
 node chack_conncet.cjs      